{
  "name": "@dsrvm/web",
  "version": "0.3.0",
  "description": "Enterprise web reference architecture v1.1: multi-tenancy (white-label host resolution), auth + RBAC + SSO (OIDC/SAML), tenant-scoped CMS, billing/usage hooks, admin console skeleton (async WebStore; in-memory + Postgres/Drizzle stores)",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "prettier --check src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dsrvm/telemetry": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.3",
    "prettier": "^3.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
