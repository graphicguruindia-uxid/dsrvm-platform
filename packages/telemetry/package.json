{
  "name": "@dsrvm/telemetry",
  "version": "0.1.0",
  "description": "Telemetry & usage metrics: tagged metrics registry, sinks (in-memory/JSONL), AI cost-per-task tracking with model pricing, CEO-facing report",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "prettier --check src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dsrvm/ai": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
